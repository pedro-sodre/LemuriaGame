#include "Lemurya.h"

int main()
{
	Lemurya jogo;
	jogo.rodar();
    //Menu menu(1280.0f, 720.0f);
    //FaseAquatica1 fase1(sf::Vector2f(VIEW_HEIGHT,VIEW_HEIGHT));
    // fase2(sf::Vector2f(VIEW_HEIGHT,VIEW_HEIGHT));
 /*   FaseNoturna3 fase3(sf::Vector2f(VIEW_HEIGHT,VIEW_HEIGHT));
*/
    return 0;
}
